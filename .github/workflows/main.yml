name: Workflow

on: [push, pull_request]

jobs:
  job_id:
    runs-on: ubuntu-latest
    steps:
      - name: Step Name
        run: echo "Hello, World!"
      - name: ip address
        run: ifconfig
      - name: who
        run: w